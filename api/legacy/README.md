# Legacy API Routes

These routes were kept for reference while the primary flow moves through `api/lead/upsert.js`.

Before deleting any of these files, confirm that no landing pages, HubSpot workflows, scripts, or external forms still call them.
